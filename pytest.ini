[pytest]
# Makes the repo root importable (db_handler, apis, event_processor, ...)
# regardless of how pytest is invoked (plain `pytest` vs `python -m pytest`),
# instead of relying on cwd happening to be on sys.path.
pythonpath = .
testpaths = tests
